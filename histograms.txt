def draw_histogram(dataframe, features, rows,cols):
	fig=plt.figure(figsize=(20,20))
	for i, feature in enumerate(features):
		ax=fig.add_subplot(rows,cols,i+1)
		dataframe[feature].hist(bins=20,ax=ax,facecolor="Pink")
		ax.set_title(feature+"Distribution",color="midnightblue")
	fig.tight_layout()
	plt.show()
draw_histogram(df,df.columns,6,3)


//matplotlib.mlab as mlab
   matplotlib.pyplot as plt
   %matplotlib inline
   

sns.pairplot(data=df) //graphs for each col
			sns=seaborn.

sns.countplot(x='Diagnosis',data=df)


sns.pairplot(df,hue='diagnosis',
	vars=['radius_mean...]

plt.figure(figsize=(20,20)0
sns.heatmap(df.corr(),annot=True,cmap='coolwarm' , linewidths=2)

plt.figure(figsize=(